"""Legal Agent Forge.

CLI *мета-агент*: вместо написания агента для автоматизации юридических
процессов вручную пользователь формулирует цель, а Forge допрашивает эту цель,
устраняет неоднозначность, фиксирует контракт ввода-вывода, синтезирует
исполняемого агента, тестирует его на реальных кейсах, дорабатывает при сбоях
и в итоге упаковывает поддерживаемый бандл, который может подхватить инженер.

Две тяжёлые возможности *заимствованы* у соседних сервисов, а не
переписаны заново:

* доступ к LLM  -> ``devassist`` (провайдер GigaChat, function calling, повторы).
* извлечение текста из документов -> ``extractors`` (40+ форматов, маршрутизация OCR).

См. :mod:`forge.integrations` — тонкие адаптеры вокруг них.
"""

from __future__ import annotations

__version__ = "0.1.0"
